#include <bits/stdc++.h>
using namespace std;

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin>>t;
    stack<char>st;

    while(t--){
        string s;
        cin>>s;
        for(int i =0;i<s.size();i++){
            if(s[i] == s[i+1]) {
                st.insert()
            }
            
        }
    }
    return 0;
}
