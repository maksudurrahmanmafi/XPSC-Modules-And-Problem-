#include <bits/stdc++.h>
using namespace std;
#define ll long long int

int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    int t;
    cin >> t;
    while (t--)
    {
        string s;
        cin >> s;
        vector<char> v;
        for (int i = 0; i < s.size()-2; i++)
        {
         cout<<s[i];
        }
        cout<<"i\n";
    }
    return 0;
}
